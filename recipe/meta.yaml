{% set version = "1.27.2" %}

package:
  name: openmeteo-sdk
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/o/openmeteo-sdk/openmeteo_sdk-{{ version }}.tar.gz
  sha256: 158444c01572a354c7a6bf1e6460040d9ed19ce44ce5ca43cf8679af2ec4148c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - python-flatbuffers ==25.9.23

test:
  imports:
    - openmeteo_sdk
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pypi.org/project/openmeteo-sdk/
  summary: Open-Meteo Python SDK
  doc_url: https://github.com/open-meteo/sdk/tree/main
  dev_url: https://github.com/open-meteo/sdk
  license: 'MIT'
  license_file: LICENSE

extra:
  recipe-maintainers:
    - akrherz
